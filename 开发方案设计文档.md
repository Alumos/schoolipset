# IP Sentinel 教师办公 IP 合规监测系统

> 开发设计文档 v0.2 · 2026-09-13

## 0. 已确认的实施约束

| 项目 | 决定 |
|---|---|
| 产品形态 | Windows 教师客户端 + VPS 服务端 + 可视化管理后台 |
| 管理后台 | React + TypeScript + Vite，采用 shadcn/ui 的组件组织方式和视觉规范 |
| Windows 兼容 | Windows 7 SP1、Windows 10、Windows 11；目标运行时为 .NET Framework 4.8 |
| Mac 开发 | Mac 只测试 API、数据库、XLSX 导入和后台 UI；不在 Mac 上运行 Windows 客户端 |
| API 端口 | VPS `18080`，供客户端 API 通信 |
| 管理端口 | VPS `18081`，供浏览器访问管理后台 |
| 部署形态 | 一个 Docker 镜像同时包含 API、管理后台静态资源和数据库迁移；Docker Compose 负责运行和持久化卷 |
| 构建 | GitHub Actions 构建 Docker 镜像和 Windows 客户端 release |
| 教师标识 | 姓名为业务主字段；系统内部保存去空白后的唯一 `name_key` |
| MVP 网络范围 | 静态 IPv4；IP、子网掩码、网关、DNS 作为一组完整配置下发 |
| 实时策略 | 客户端默认 60 秒心跳；网络地址变化、睡眠恢复、配置变更后立即检测；后台使用 SSE 实时刷新 |

姓名是业务上的主要字段，但不能单独作为长期设备身份。首次注册后由本机生成设备 ID 并绑定设备；默认同一教师只能保留一个未撤销的活动设备，换机时由管理员撤销旧设备后重新绑定。这样仍然只需要教师输入姓名，但不会让每次心跳依赖姓名认证。

## 1. 产品目标与范围

### 1.1 目标

学校管理员维护教师与固定办公 IP 的对应关系。Windows 客户端在教师电脑上运行后，定期采集当前活动物理网卡的 IPv4、子网掩码、网关、DNS、MAC 哈希、主机名和客户端版本，向服务端上报并判断是否与名单一致。

发现不一致时，客户端展示当前配置和目标配置，建议教师一键修改。教师确认后，客户端一次性设置 IP、子网掩码、网关和 DNS，并执行连通性验证；验证失败时恢复原配置并将结果上报后台。教师选择不修改时，后台继续标记为异常/拒绝修改。

### 1.2 不在 MVP 内

- DHCP 自动分配策略、IPv6、代理认证和域控自动注册。
- 远程强制修改教师电脑网络配置。
- 以 MAC 地址作为放行凭证或网络准入控制。
- 绕过 UAC、隐藏运行、进程注入、键盘监听或安全软件对抗。
- 多实例横向扩容；单个 VPS 实例面向当前学校规模运行。

## 2. 用户与状态定义

### 2.1 用户

- 管理员：登录后台、导入和维护名单、查看实时状态、撤销设备绑定、查看审计记录。
- 教师：运行客户端、输入姓名、查看自己的配置差异、明确决定是否一键修改。

### 2.2 教师状态

| 状态 | 含义 | 后台处理 |
|---|---|---|
| `合规` | 当前 IP、掩码、网关、DNS 均匹配 | 正常 |
| `IP 不一致` | 心跳上报配置与名单不一致 | 需要关注 |
| `修改中` | 教师已点击一键修改，正在等待客户端验证结果 | 暂停显示“拒绝修改” |
| `已拒绝修改` | 教师明确选择不修改或关闭建议 | 需要关注 |
| `修改失败` | 目标配置验证失败，原配置已恢复 | 需要关注 |
| `疑似 IP 冲突` | 修改后网络验证失败，客户端已恢复原配置 | 需要管理员确认目标 IP |
| `回滚失败` | 新配置和旧配置都未能恢复 | 高优先级人工处理 |
| `离线` | 超过 3 分钟没有心跳 | 需要关注 |
| `未绑定` | 有名单但尚未绑定设备 | 等待教师首次启动 |

“疑似 IP 冲突”是证据等级，不是绝对结论。ICMP 可能被禁用、DNS 可能异常、百度可能临时不可达，后台必须同时显示网关、DNS、TCP/HTTPS、百度 ping 等探测结果。

## 3. 总体架构

```text
Windows 客户端
  ├─ 物理网卡探测 / 心跳 / 本地队列
  ├─ 设备 ID + 设备令牌认证
  └─ UAC 子进程：netsh 设置与回滚
          │ HTTP(S) :18080
          ▼
单体 Node.js 服务进程
  ├─ API 监听 18080
  ├─ 管理后台静态资源监听 18081
  ├─ SQLite WAL 数据库 / 迁移 / 审计
  └─ SSE 管理端实时事件
          │
          ▼
浏览器管理后台（shadcn/ui 视觉体系）
```

### 3.1 单 Docker 镜像

镜像包含：

1. Node.js API 服务。
2. React/Vite 构建后的管理后台静态文件。
3. SQLite 数据库初始化逻辑。
4. 健康检查接口。

容器内同一进程监听两个端口：

- `18080`：设备接口、管理员 API、健康检查。
- `18081`：仅提供管理后台页面，浏览器再跨端口请求 `18080` 的管理员 API。

数据库文件放在 `/data/schoolipset.sqlite`，通过 Docker volume 持久化。当前规模约 182 名教师，SQLite WAL 足够支撑单实例；将来若需要多副本或大量管理员并发，再迁移 PostgreSQL，API 契约不变。

### 3.2 HTTP 与传输边界

当前 MVP 采用普通 JSON 通信，不要求配置 RSA/AES 密钥。客户端使用设备 ID、短期设备令牌和幂等键完成注册、心跳及修改结果上报；服务端只保存令牌哈希。客户端本地的设备状态仍使用 Windows DPAPI 保护。

这是一种简化部署方案，不等同于传输加密。由于 API 和后台当前使用 HTTP，管理员登录密码、会话令牌和设备令牌可能被同一网络中的监听者看到。因此将端口暴露到公网前，仍建议在 VPS 前面配置 HTTPS、VPN 或 SSH 隧道；若暂时只使用公网 IP 和 HTTP，应明确接受这个风险。

## 4. 管理后台设计

### 4.1 视觉方向

使用 shadcn/ui 的可复制组件模式（Button、Badge、Dialog、Input、Table、Tabs、Toast、DropdownMenu），Tailwind CSS 负责布局和主题变量。整体风格：深墨色侧栏、米白画布、薄边框卡片、薄荷绿为主强调色、玫红/琥珀色表示风险和处理中状态。

设计原则：

- 2026 年主流 SaaS 风格：轻阴影、大圆角、清晰留白、信息密度适中。
- 桌面端优先，同时保证平板和窄屏可查看。
- 状态必须有文字，不能只用颜色；满足色觉差异场景。
- 所有危险操作二次确认，并在 Toast 中给出结果。
- 后台顶部展示实时连接、最后刷新时间和当前部署端口。

### 4.2 页面

#### 监测总览

- 纳管教师数、当前在线数、合规数、需要关注数。
- 合规率环形图和最近 7 天活动趋势。
- 最近检测事件表：教师、当前位置、当前 IP、状态、时间。
- “导入最新名单”主操作入口。
- 无数据时显示引导，而不是空白表格。

#### 教师名单

- 姓名、地点、目标 IP/前缀、网关、DNS、设备主机名、客户端版本、最近心跳、状态。
- 姓名、地点、IP 搜索；状态筛选。
- 查看教师的当前配置与目标配置。
- 撤销设备绑定；撤销后下一次客户端请求必须重新注册。

#### 检测事件

- 按时间倒序展示完整历史。
- 展示当前配置、目标结果、验证证据和失败原因。
- 支持区分 `IP 不一致`、`已拒绝修改`、`疑似 IP 冲突`、`回滚失败`。

#### 导入中心

- 拖放/选择 XLSX。
- 解析后先展示 SHA-256、表头识别、总行数、可导入行数、错误行数和警告行数。
- 错误行阻止提交；提交使用一次数据库事务。
- 默认增量新增/更新；“按文件全量同步”必须显式勾选。
- 保存导入人、时间、原文件哈希、原始文件名和结果。

#### 系统设置（后续）

- 管理员账号、TOTP、角色权限。
- 心跳周期、离线阈值、百度 ping 是否强制、设备数量上限。
- 客户端发布版本和灰度策略。

## 5. XLSX 导入规则

目录中的实际文件当前结构如下：

- 第 1 行：合并标题“夏驾河小学教师办公计算机固定网络IP分配”。
- 第 2 行表头：`序号、教职工、IP地址、子网掩码、网关、DNS、地点`。
- 当前有效数据：182 行。
- 当前掩码均为 `255.255.255.0`，导入后转换为前缀 `/24`。
- 当前 DNS 均为 `10.150.150.150`。
- `地点`保存为位置字段，不当作网卡名称。

### 5.1 字段映射

| 标准字段 | 支持的原始列名 | 规则 |
|---|---|---|
| `name` | 教师姓名、教职工、姓名、教师 | 必填；NFKC 规范化；去除首尾和姓名内部空白形成唯一键 |
| `ip` | IP、IP地址、IPv4 | 必填；只接受 IPv4 |
| `prefix` | 前缀长度、前缀、子网掩码、掩码 | 支持 `24`、`/24`、`255.255.255.0` |
| `gateway` | 网关、默认网关 | 必填；必须与 IP 在同一网段 |
| `dns` | DNS、DNS服务器 | 必填；支持逗号、空格、分号分隔多个 IPv4 |
| `location` | 地点、位置、楼栋、办公地点 | 可选；原样保留 |
| `interfaceHint` | 网卡提示、网卡、接口、网络接口 | 可选；不与地点混用 |
| `enabled` | 启用、是否启用、状态 | 可选；缺省为启用 |

校验包括：空姓名、IP/掩码/网关/DNS 合法性、网关同网段、IP 重复、姓名规范化后重复。文件中没有出现的记录默认不变，只有显式选择全量同步才停用。

真实学校表格属于敏感数据，不提交到公开 Git 仓库。仓库只保留脱敏 fixture；真实文件通过后台上传或 VPS 管理操作导入。

## 6. Windows 客户端设计

### 6.1 首次运行与设备绑定

1. 客户端显示用途、采集字段、保存期限和管理员信息。
2. 教师输入姓名；服务端按唯一姓名查找已启用记录。
3. 客户端在本机生成随机设备 ID。
4. 设备 ID 和设备令牌使用 Windows DPAPI（CurrentUser）保护后保存到本地应用目录。
5. 客户端向服务端注册设备，服务端返回短期设备令牌和当前目标配置。
6. 已绑定设备再次启动时无需重复输入；设备撤销或令牌失效时要求重新注册。

姓名是方便的业务主字段，不是设备认证本身。默认一个姓名只允许一个未撤销设备，防止其他人仅凭输入同名教师姓名获取目标配置。

### 6.2 网卡选择

默认枚举：

- `OperationalStatus.Up` 的 Ethernet 和 Wireless80211 网卡；
- 排除回环、Tunnel、VPN、虚拟机、Docker、WireGuard 等关键词；
- 读取 IPv4、前缀/掩码、默认网关、DNS、物理 MAC；
- 多个符合条件时优先有默认网关的网卡；
- 后台提供 `interfaceHint` 时优先匹配接口名称。

客户端上报 MAC 前标准化为 12 位十六进制；服务端使用管理员密码派生 HMAC，仅保存 MAC 哈希，不保存原始 MAC。

### 6.3 监测周期

- 默认心跳：60 秒，可带 0～10 秒随机抖动。
- Windows `NetworkChange.NetworkAddressChanged` 触发立即检测。
- 系统从睡眠恢复时立即检测。
- 心跳响应携带策略版本；目标名单版本变化时立即重新读取。
- 连续 3 分钟没有心跳，后台标记离线。
- 断网时事件进入本地队列；恢复后按时间顺序补报，队列限制大小并丢弃最旧的低价值重复心跳，不能丢失修改结果和回滚失败结果。

### 6.4 网络一键修改事务

教师点击“一键修改”后：

1. 客户端向服务端请求一次性 change token。
2. 服务端只返回数据库中完整的 IP、前缀、网关、DNS；任何字段缺失都拒绝下发。
3. 客户端记录接口名和当前网络快照：DHCP 状态、IP、掩码、网关、DNS、MAC。
4. 客户端请求 UAC 提升；普通运行阶段不需要管理员权限。
5. 使用 `netsh interface ipv4` 设置静态 IPv4 和 DNS；接口名使用系统读取到的真实名称，不能使用地点字段。
6. 设置后重新读取网卡配置，确认四项均与目标一致。
7. 执行连通性验证。
8. 验证通过：上报成功，后台状态变为合规。
9. 验证失败：立即尝试恢复原配置；恢复成功则上报失败原因和证据；恢复失败则上报高优先级回滚失败。

目标配置不可由教师在客户端自由编辑，客户端只执行服务端签发的 change token 对应的配置。

### 6.5 连通性验证

用户明确要求修改后 `ping baidu.com` 验证，因此默认验证包含：

| 探测 | 用途 |
|---|---|
| 重新读取配置 | 确认 IP、掩码、网关、DNS 已真正写入 |
| ping 默认网关 | 判断本地网段、ARP 和网关基本连通 |
| DNS 解析 `baidu.com` | 判断 DNS 是否可用 |
| TCP 443 连接 `baidu.com` | 判断 HTTPS 出口是否可用，避免只依赖 ICMP |
| ping `baidu.com` | 满足学校策略要求并记录结果 |

默认判定：配置读取正确、网关可达、DNS 解析成功、TCP 443 成功且百度 ping 成功，才算验证通过。百度 ping 失败会触发回滚并上报“疑似目标 IP 被占用/ICMP 不可达”；后台同时展示其他探测结果，避免把它误写成确定事实。

如学校网络禁止 ICMP，应在后台策略中关闭“百度 ping 必须成功”，保留网关、DNS 和 HTTPS 验证，否则所有客户端都会被误判。

### 6.6 失败状态上报

客户端上报：

- 修改前配置快照；
- 目标配置版本；
- 修改后读取到的配置；
- 网关、DNS、TCP、百度 ping 结果；
- netsh 错误码和摘要；
- 是否完成回滚；
- 客户端版本、设备 ID 和时间戳。

不上传完整原始日志中的用户隐私内容；错误详情限制长度。

## 7. API 设计

所有 JSON 时间使用 UTC ISO 8601；所有设备请求带幂等键。客户端和 API 使用普通 JSON，不再需要应用层加密 envelope。

### 7.1 管理端接口

| 方法 | 路径 | 作用 |
|---|---|---|
| POST | `/v1/admin/login` | 管理员登录，返回短期 Bearer 会话 |
| GET | `/v1/admin/overview` | 总览统计 |
| GET | `/v1/admin/teachers` | 教师名单、分配配置和设备状态 |
| GET | `/v1/admin/events` | 检测事件 |
| GET | `/v1/admin/audit-logs` | 审计日志 |
| POST | `/v1/admin/import/preview` | 上传并解析 XLSX，不落名单 |
| POST | `/v1/admin/import/commit` | 提交预览，事务写入 |
| POST | `/v1/admin/teachers/{id}/revoke-devices` | 撤销设备绑定 |
| GET | `/v1/admin/stream` | SSE 实时事件流 |

### 7.2 客户端接口

| 方法 | 路径 | 作用 |
|---|---|---|
| POST | `/v1/device/register` | 姓名和设备 ID 注册 |
| POST | `/v1/heartbeat` | 上报当前网络和运行状态 |
| GET | `/v1/device/policy` | 获取当前策略和名单版本 |
| POST | `/v1/change-requests` | 教师已确认后获取一次性修改令牌 |
| POST | `/v1/change-requests/{id}/result` | 上报修改、验证和回滚结果 |

### 7.3 心跳请求示例

```json
{
  "deviceKey": "device-guid",
  "token": "short-lived-device-token",
  "hostname": "TEACHER-PC",
  "clientVersion": "0.2.0",
  "mac": "00-11-22-33-44-55",
  "interfaceName": "以太网",
  "ip": "10.168.160.11",
  "prefix": 24,
  "gateway": "10.168.160.254",
  "dns": ["10.150.150.150"],
  "idempotencyKey": "uuid",
  "source": "client",
  "clientStatus": "normal"
}
```

服务端收到后进行令牌、设备、IP 格式、前缀、DNS 数量和幂等性校验；客户端传入的 `clientStatus` 只能取受控枚举，不能直接伪造任意后台状态。

## 8. 数据模型

本期 SQLite 表结构：

```text
teachers
  id, name, name_key(unique), enabled, location, created_at, updated_at

ip_assignments
  id, teacher_id(unique), interface_hint, ip, prefix, gateway, dns_json,
  enabled, effective_from, effective_to, updated_at

devices
  id, teacher_id, device_key(unique), public_key, token_hash(unique), hostname,
  mac_hash, client_version, first_seen, last_seen, revoked_at

check_events
  id, teacher_id, device_id, observed_ip, observed_prefix, observed_gateway,
  observed_dns_json, observed_mac_hash, result, reason, source,
  idempotency_key(unique), created_at

change_requests
  id, event_id, device_id, target_config_json, previous_config_json,
  change_token_hash, status, verification_json, created_at, completed_at

audit_logs
  id, actor, action, object_type, object_id, metadata_json, created_at

import_batches
  id, file_sha256, original_filename, imported_by, row_count, error_count,
  full_sync, created_at
```

约束：外键开启、WAL、唯一姓名键、唯一设备键、唯一心跳幂等键、所有敏感操作审计。MAC 只保存 HMAC，不保存原始值；`public_key` 为旧版本遗留字段，当前版本不再使用。

## 9. 安全、隐私与审计

### 9.1 管理端

- 生产管理员登录建议使用 HTTPS 或 VPN/SSH 隧道；当前简化版的密码和令牌通过普通 HTTP 传输。
- 管理员密码由 Compose 的 `environment.ADMIN_PASSWORD` 手动设置；生产环境禁止使用默认密码且至少 8 个字符。
- 后续加入 TOTP、RBAC 和登录失败限流。
- CORS 只允许管理端来源；请求体和文件大小限制；账号/IP 限流。
- 普通管理员不可删除审计日志；只允许按保留策略归档。
- SQLite 每日备份，备份文件加密，默认保存至少 30 天。

### 9.2 客户端

- 只有修改网络时触发 UAC。
- 设备令牌短期有效、可撤销，服务端只保存哈希。
- 设备 ID 和令牌使用 DPAPI 保护；本地离线队列限制大小。
- 不读取键盘、不注入进程、不隐藏窗口、不自修改、不绕过杀毒软件。

### 9.3 隐私告知

首次运行必须告知：采集姓名、设备主机名、客户端版本、当前 IP、网卡 MAC 的用途；MAC 仅用于设备记录并以 HMAC 保存；管理员身份、保存期限、撤销渠道和网络修改行为。学校需要在授权范围内使用，并按适用法律和校内制度确定保存期限。

## 10. Docker Compose 部署

```yaml
services:
  schoolipset:
    image: ghcr.milu.moe/alumos/schoolipset:latest
    restart: unless-stopped
    environment:
      NODE_ENV: production
      API_PORT: 18080
      ADMIN_PORT: 18081
      DB_PATH: /data/schoolipset.sqlite
      ADMIN_PASSWORD: 替换为自己的随机强密码
      ADMIN_ORIGINS: http://139.196.136.61:18081
    ports:
      - "18080:18080"
      - "18081:18081"
    volumes:
      - schoolipset_data:/data

volumes:
  schoolipset_data:
```

只需要把 `ADMIN_PASSWORD` 替换为自己的密码，不需要 `.env`、`MAC_HASH_SECRET`、`CLIENT_CRYPTO_REQUIRED` 或任何 `DEVICE_SERVER_*` 密钥。

VPS 防火墙只开放必要端口和 SSH。镜像使用固定 SHA，不直接依赖可变 `latest`；更新前先备份 `/data`，更新后访问 `/health` 和 `/healthz`。

由于应用自身只负责 HTTP 监听，TLS 由 VPS 上的 Caddy/Nginx/Traefik 或 VPN 层提供。管理端不要长期用公网明文 `http://IP:18081` 登录。

## 11. GitHub Actions

### 11.1 PR

- `npm ci`。
- TypeScript 类型检查。
- API/导入工具单元测试。
- React/Vite 生产构建。
- Docker 镜像构建验证，不推送。
- 依赖审计和 Secret 扫描。

### 11.2 主分支

- 构建单体 Docker 镜像。
- 推送 `ghcr.io/alumos/schoolipset:sha-<commit>`。
- 可选更新 `latest`，部署端仍使用固定 SHA。
- 生成镜像摘要、SBOM 和构建摘要。

### 11.3 版本 tag

- 构建 Windows 客户端，目标 .NET Framework 4.8。
- 客户端 API 地址直接写入 EXE，不需要通过 GitHub Secret 注入公钥。
- 生成 `IP-Sentinel-Windows.exe` 单文件，不再生成 ZIP。
- 使用代码签名证书签署 EXE、安装包和更新清单（证书不提交仓库）。
- 只发布 `IP-Sentinel-Windows.exe` 单文件；不再打 ZIP，也不需要额外的 Newtonsoft.Json DLL 或配置文件。构建摘要记录 EXE 的 SHA-256。
- `.NET Framework 4.8` 仍是 Windows 系统前置条件，不能随业务 EXE 一起内嵌；发布页需明确这一点。
- 创建 GitHub Release，并附带 SHA-256。

GitHub Actions 是 Windows 客户端的主要构建环境；Mac 本地只验证服务端和管理后台，不能将 Mac 构建成功等同于 Win7 网络修改验证成功。

## 12. 360/杀毒误报控制

个人开发者无法保证任何杀毒软件永不误报，但可以降低风险：

- 使用固定产品名、发布者、版本号和安装目录。
- 为 EXE、安装包和更新包使用有效代码签名证书并加时间戳。
- 依赖公开、可审计；不使用混淆、壳、进程注入、键盘钩子和自修改。
- 安装、更新、卸载、日志目录对用户可见。
- 更新包使用 HTTPS、签名清单、SHA-256 和用户确认。
- 发布页提供版本、哈希、变更记录和隐私政策。
- 出现误报后提交 360/Defender 申诉材料、签名信息和可复现构建信息。

## 13. 测试策略

### 13.1 Mac 可执行测试

- API 健康检查、管理员登录、限流和鉴权。
- 实际 XLSX 的表头识别、掩码转换、182 行导入、重复校验、事务回滚。
- 设备注册、心跳幂等、完整配置匹配、状态统计。
- 修改请求 token、结果上报、疑似 IP 冲突状态和 SSE。
- 后台生产构建、响应式布局和导入交互。

### 13.2 Windows CI/虚拟机测试

至少在 Windows 7 SP1、Windows 10、Windows 11 验证：

- .NET Framework 4.8 检测、安装和卸载。
- 首次姓名注册、设备令牌保存和撤销后重绑。
- 以太网/Wi-Fi 网卡识别和虚拟网卡排除。
- UAC 提权、中文网卡名称和 `netsh` 参数。
- IP、掩码、网关、DNS 一起修改。
- 百度 ping 成功、ICMP 被禁用、DNS 失败、网关不可达等场景。
- 修改失败自动恢复原配置，回滚失败告警。
- 断网 24 小时、本地队列恢复、重复事件幂等。
- 360/Defender 扫描、签名和更新流程。

真实网络修改必须在隔离测试网或虚拟机快照内执行，避免误改开发机网络。

## 14. 开发里程碑

1. **基础骨架**：单体 Node 服务、SQLite、shadcn/ui 后台壳、Docker 构建。
2. **名单与总览**：XLSX 预览/事务导入、教师名单、总览统计、审计。
3. **客户端协议**：设备注册、设备令牌、普通 JSON 心跳、策略版本、SSE 实时刷新。
4. **Windows 客户端**：Win7/10/11 兼容 UI、网卡探测、UAC、netsh 配置、验证与回滚。
5. **发布准备**：代码签名、GitHub Release、固定 SHA 部署、备份恢复、误报申诉材料。

## 15. 验收标准

- 后台可导入当前 XLSX，正确识别 182 条记录，`255.255.255.0` 转为 `/24`。
- 同一教师姓名和 IP 的重复数据无法提交。
- 心跳在服务端落库并在后台显示不超过 10 秒（不含客户端调度等待）。
- 名单修改在客户端下一个周期内生效，默认不超过 60 秒；配置变更事件可立即触发。
- 合规判断至少比较 IP、掩码、网关、DNS。
- 教师点击修改后，后台进入“修改中”，不再显示“拒绝修改”；最终以客户端验证结果收敛为合规、失败、疑似冲突或回滚失败。
- 网络修改必须成组设置 IP、掩码、网关、DNS；验证失败自动恢复原配置并上报。
- 百度 ping 失败场景有明确的验证证据和回滚记录。
- 重复心跳、重复修改结果幂等；断网恢复后关键结果不丢失。
- MAC 不以原文落库；所有敏感操作有审计。
- GitHub Actions 可构建单体 Docker 镜像和 Windows 客户端 release。

## 16. 上线前仍需确认

这些不阻塞第一版开发，但上线前必须明确：

1. 管理后台使用域名 HTTPS，还是只通过 VPN/SSH 隧道访问。
2. 是否接受公网 HTTP 传输风险；如不接受，配置 HTTPS、VPN 或 SSH 隧道。
3. 是否允许同一教师绑定多台电脑。
4. 学校网络是否允许 ICMP；若不允许，关闭百度 ping 强制条件。
5. MAC、原始检测事件、审计日志和备份的具体保存期限。
6. 是否需要 TOTP、多个管理员角色和操作审批。
7. Win7 SP1 是否有真实测试机或虚拟机快照。
8. 代码签名证书预算及发布者名称。
